UIBuilder
=========

Installation
-----------

    npm install -g gulp
    npm install

Build project
-----------
    gulp build

Build css(Production version)
-----------
    gulp buildCss-production
    
Build css(Development version)
-----------
    gulp buildCss-dev

Build documentation
-----------
    gulp doc-build
